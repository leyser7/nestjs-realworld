import { BackendValidationPipePipe } from './backend-validation-pipe.pipe';

describe('BackendValidationPipePipe', () => {
  it('should be defined', () => {
    expect(new BackendValidationPipePipe()).toBeDefined();
  });
});
